.header__nav {
    background-image: url(../img/coffeebeans-1500x500.png);
    background-position: 323% 70%;
    background-color: transparent;
}
.header__nav {
    padding: 5px;
}
.header__logo {
    display: flex;
    background-color: var(--bg-color);
    width: 55px;
}
.header__menu {
    font-size: 0.8rem;
    font-size: bold;
    text-transform: uppercase;
    display: flex;
    flex-direction: column;
    align-content: flex-end;
    align-items: flex-end
}
.header__menu__item {
    color: var(--main-text-color);
    text-decoration: none;
    background-color: var(--bg-color);
}
.header__menu__item:hover {
    text-decoration: underline;
}
@media (min-width: 500px) {
    .header__nav {
        display:flex;
    }
    .header__menu {
        flex-direction: row;
        align-items: center;
        flex: 1;
        justify-content: space-evenly;
    }
}